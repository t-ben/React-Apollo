import React from 'react';
import ReactDOM from 'react-dom';

class App extends React.Component {
  render() {
    return (
      <div>
        <h3>Hello React boilerplate</h3>
        <p>just a simple starter with hot reloading that works on windows.</p>
        <p>no css or routing yet.</p>
        <p>may need to clean some of the dev dependencies</p>
      </div>
    )
  }
}

export default App;